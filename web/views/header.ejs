<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= typeof title !== 'undefined' ? title : 'Hụi — rotating savings circles' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;600;800&family=Faustina:wght@600;700&display=swap">
  <link rel="stylesheet" href="/style.css">
  <script>window.CONFIG = <%- JSON.stringify(config) %>;</script>
</head>
<body>
<header class="topbar">
  <a class="brand" href="/dashboard">
    <svg width="22" height="22" viewBox="0 0 22 22" aria-hidden="true">
      <circle cx="11" cy="11" r="8.4" fill="none" stroke="currentColor" stroke-opacity=".45" stroke-width="1.4"/>
      <circle cx="11" cy="2.6" r="2.6" fill="#e0a32c"/>
      <circle cx="18.4" cy="15.2" r="1.7" fill="currentColor" fill-opacity=".55"/>
      <circle cx="3.6" cy="15.2" r="1.7" fill="currentColor" fill-opacity=".55"/>
    </svg>
    <span>Hụi</span>
  </a>
  <nav id="nav" hidden>
    <span id="who" class="who"></span>
    <button class="link-dark" id="signout" type="button">Sign out</button>
  </nav>
</header>
<main>
